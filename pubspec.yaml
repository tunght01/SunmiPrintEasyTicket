name: sunmi_print_ticket
description: This is a printer product
homepage: https://github.com/tunght01/SunmiPrintEasyTicket


version: 1.0.7

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:

  plugin:
    platforms:
      android:
        package: com.example.easy_ticket_b08
        pluginClass: SunmiPrinterPlugin

